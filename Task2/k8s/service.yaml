apiVersion: v1
kind: Service

metadata:
  name: insuretech-app
  namespace: insuretech

spec:
  type: NodePort

  selector:
    app: insuretech-app

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP